=== Comparison of FAT and SLIM Docker Images ===

📦 FAT Image:
- Repository: fat-model
- Base image: python:3.9
- Size: 31.9 GB
- Layers: many (включає повний Python + build tools + кеш бібліотек)
- Contains: системні утиліти, компілятори, зайві пакети
- Pros: простий у створенні
- Cons: великий розмір, повільна збірка, неефективний для продакшну

🐋 SLIM Image:
- Repository: slim-model
- Base image: python:3.9-slim
- Size: 11.4 GB
- Layers: мінімум (тільки необхідні залежності)
- Contains: лише Python runtime і ML-бібліотеки (torch, torchvision, pillow)
- Pros: швидше завантажується, менше займає місця, ідеально для inference
- Cons: потребує ручного додавання build-залежностей (gcc тощо)

🧩 Key differences:
- SLIM в ~3 рази менший за FAT.
- FAT-образ містить повне середовище розробки, тоді як SLIM — лише для запуску.
- SLIM підходить для деплою, FAT — для локальної розробки або відладки.

💡 Possible optimizations:
- Використати базовий образ `python:3.9-alpine` або distroless.
- Використати `torchvision` без CUDA, якщо GPU не потрібен.
- Очистити кеш (`--no-cache-dir`) і тимчасові файли (`apt clean`, `rm -rf /var/lib/apt/lists/*`).
